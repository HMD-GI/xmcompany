<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xm.mapper.SalaryMapper">
    <!-- 基础查询SQL片段 -->
    <sql id="baseSalaryQuery">
        SELECT
            s.id,
            s.employee_id,
            s.employee_name,
            s.bank_card_no,
            s.bank_name,
            s.basic_salary,
            s.performance_base,
            s.allowance,
            s.insurance_amount,
            s.effective_date,
            s.status,
            s.create_time,
            s.update_time
        FROM employee_salary s
        WHERE 1=1
    </sql>

    <!-- 分页查询薪资配置列表 -->
    <select id="selectSalaryPage" resultType="com.xm.entity.Salary">
        <include refid="baseSalaryQuery"/>
        <if test="query != null">
            <if test="query.employeeId != null">
                AND s.employee_id = #{query.employeeId}
            </if>
            <if test="query.employeeName != null and query.employeeName != ''">
                AND s.employee_name LIKE CONCAT('%', #{query.employeeName}, '%')
            </if>
            <if test="query.bankCardNo != null and query.bankCardNo != ''">
                AND s.bank_card_no LIKE CONCAT('%', #{query.bankCardNo}, '%')
            </if>
            <if test="query.status != null">
                AND s.status = #{query.status}
            </if>
        </if>
        ORDER BY s.create_time DESC
    </select>
</mapper>